# CareBridge Environment Configuration
ENVIRONMENT=development
APP_NAME="CareBridge Emergency Backend"

# Supabase Credentials (Optional for local testing; mock fallback is active if empty)
SUPABASE_URL=
SUPABASE_KEY=

# Bhashini API Credentials (Optional for local testing; smart local Hinglish translation active if empty)
BHASHINI_API_KEY=
BHASHINI_USER_ID=
BHASHINI_PIPELINE_ID=
BHASHINI_ENDPOINT=https://dhruva-api.bhashini.gov.in/services/inference/pipeline

# Resilient Hackathon Mode
USE_MOCK_FALLBACK_ON_ERROR=true
